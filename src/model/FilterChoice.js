/**
 * BuiltByBit API
 * All operations not tagged 'free' require an active [Ultimate](https://builtbybit.com/account/ultimate) subscription or invite-only permissions.
 *
 * The version of the OpenAPI document: v2
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The FilterChoice model module.
 * @module model/FilterChoice
 * @version v2
 */
class FilterChoice {
    /**
     * Constructs a new <code>FilterChoice</code>.
     * @alias module:model/FilterChoice
     * @param key {String} 
     * @param queryField {String} This is the query string field name to use when attempting to filter on this option. Pass this name as the query string field name and the 'option_key' value as the query string field key.    Eg, if 'option_key' = 'action' and 'query_field' = 'rf[genre]', you should be the query string as `?rf[genre]=action`.
     * @param title {String} 
     */
    constructor(key, queryField, title) { 
        
        FilterChoice.initialize(this, key, queryField, title);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, key, queryField, title) { 
        obj['key'] = key;
        obj['query_field'] = queryField;
        obj['title'] = title;
    }

    /**
     * Constructs a <code>FilterChoice</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/FilterChoice} obj Optional instance to populate.
     * @return {module:model/FilterChoice} The populated <code>FilterChoice</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new FilterChoice();

            if (data.hasOwnProperty('key')) {
                obj['key'] = ApiClient.convertToType(data['key'], 'String');
            }
            if (data.hasOwnProperty('count')) {
                obj['count'] = ApiClient.convertToType(data['count'], 'Number');
            }
            if (data.hasOwnProperty('query_field')) {
                obj['query_field'] = ApiClient.convertToType(data['query_field'], 'String');
            }
            if (data.hasOwnProperty('title')) {
                obj['title'] = ApiClient.convertToType(data['title'], 'String');
            }
            if (data.hasOwnProperty('selected')) {
                obj['selected'] = ApiClient.convertToType(data['selected'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>FilterChoice</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>FilterChoice</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of FilterChoice.RequiredProperties) {
            if (!data[property]) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['key'] && !(typeof data['key'] === 'string' || data['key'] instanceof String)) {
            throw new Error("Expected the field `key` to be a primitive type in the JSON string but got " + data['key']);
        }
        // ensure the json data is a string
        if (data['query_field'] && !(typeof data['query_field'] === 'string' || data['query_field'] instanceof String)) {
            throw new Error("Expected the field `query_field` to be a primitive type in the JSON string but got " + data['query_field']);
        }
        // ensure the json data is a string
        if (data['title'] && !(typeof data['title'] === 'string' || data['title'] instanceof String)) {
            throw new Error("Expected the field `title` to be a primitive type in the JSON string but got " + data['title']);
        }

        return true;
    }


}

FilterChoice.RequiredProperties = ["key", "query_field", "title"];

/**
 * @member {String} key
 */
FilterChoice.prototype['key'] = undefined;

/**
 * The dynamic count of relevant resources based on the already-applied filter options.  This may be *null* if we're unable to provide a count for a specific filter combination.
 * @member {Number} count
 */
FilterChoice.prototype['count'] = undefined;

/**
 * This is the query string field name to use when attempting to filter on this option. Pass this name as the query string field name and the 'option_key' value as the query string field key.    Eg, if 'option_key' = 'action' and 'query_field' = 'rf[genre]', you should be the query string as `?rf[genre]=action`.
 * @member {String} query_field
 */
FilterChoice.prototype['query_field'] = undefined;

/**
 * @member {String} title
 */
FilterChoice.prototype['title'] = undefined;

/**
 * Whether or not this option was selected, but only applicable where type = `radio` or `checkbox`.
 * @member {Boolean} selected
 */
FilterChoice.prototype['selected'] = undefined;






export default FilterChoice;

