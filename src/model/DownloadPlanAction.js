/**
 * BuiltByBit API
 * All operations not tagged 'free' require an active [Ultimate](https://builtbybit.com/account/ultimate) subscription or invite-only permissions.    V2 documentation: https://builtbybit.com/wiki/api-v2/ \\  OAuth2 documentation: https://builtbybit.com/wiki/oauth2/
 *
 * The version of the OpenAPI document: v2
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The DownloadPlanAction model module.
 * @module model/DownloadPlanAction
 * @version v2
 */
class DownloadPlanAction {
    /**
     * Constructs a new <code>DownloadPlanAction</code>.
     * @alias module:model/DownloadPlanAction
     * @param type {module:model/DownloadPlanAction.TypeEnum} 
     */
    constructor(type) { 
        
        DownloadPlanAction.initialize(this, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, type) { 
        obj['type'] = type;
    }

    /**
     * Constructs a <code>DownloadPlanAction</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DownloadPlanAction} obj Optional instance to populate.
     * @return {module:model/DownloadPlanAction} The populated <code>DownloadPlanAction</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DownloadPlanAction();

            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
            if (data.hasOwnProperty('src')) {
                obj['src'] = ApiClient.convertToType(data['src'], 'String');
            }
            if (data.hasOwnProperty('dst')) {
                obj['dst'] = ApiClient.convertToType(data['dst'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DownloadPlanAction</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DownloadPlanAction</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of DownloadPlanAction.RequiredProperties) {
            if (!data[property]) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }
        // ensure the json data is a string
        if (data['src'] && !(typeof data['src'] === 'string' || data['src'] instanceof String)) {
            throw new Error("Expected the field `src` to be a primitive type in the JSON string but got " + data['src']);
        }
        // ensure the json data is a string
        if (data['dst'] && !(typeof data['dst'] === 'string' || data['dst'] instanceof String)) {
            throw new Error("Expected the field `dst` to be a primitive type in the JSON string but got " + data['dst']);
        }

        return true;
    }


}

DownloadPlanAction.RequiredProperties = ["type"];

/**
 * @member {module:model/DownloadPlanAction.TypeEnum} type
 */
DownloadPlanAction.prototype['type'] = undefined;

/**
 * The source file/directory.
 * @member {String} src
 */
DownloadPlanAction.prototype['src'] = undefined;

/**
 * A destination file/directory relative to the server root.
 * @member {String} dst
 */
DownloadPlanAction.prototype['dst'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
DownloadPlanAction['TypeEnum'] = {

    /**
     * value: "copy"
     * @const
     */
    "copy": "copy",

    /**
     * value: "copy_dir"
     * @const
     */
    "copy_dir": "copy_dir",

    /**
     * value: "open_zip"
     * @const
     */
    "open_zip": "open_zip",

    /**
     * value: "server_restart"
     * @const
     */
    "server_restart": "server_restart"
};



export default DownloadPlanAction;

