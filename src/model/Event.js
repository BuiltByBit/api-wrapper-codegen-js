/**
 * BuiltByBit API
 * All operations not tagged 'free' require an active [Ultimate](https://builtbybit.com/account/ultimate) subscription or invite-only permissions.
 *
 * The version of the OpenAPI document: v2
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The Event model module.
 * @module model/Event
 * @version v2
 */
class Event {
    /**
     * Constructs a new <code>Event</code>.
     * @alias module:model/Event
     * @param eventId {Number} 
     * @param listenerId {Number} 
     * @param createdAt {Number} 
     * @param triggerType {String} 
     * @param triggerData {Object} 
     */
    constructor(eventId, listenerId, createdAt, triggerType, triggerData) { 
        
        Event.initialize(this, eventId, listenerId, createdAt, triggerType, triggerData);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, eventId, listenerId, createdAt, triggerType, triggerData) { 
        obj['event_id'] = eventId;
        obj['listener_id'] = listenerId;
        obj['created_at'] = createdAt;
        obj['trigger_type'] = triggerType;
        obj['trigger_data'] = triggerData;
    }

    /**
     * Constructs a <code>Event</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Event} obj Optional instance to populate.
     * @return {module:model/Event} The populated <code>Event</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Event();

            if (data.hasOwnProperty('event_id')) {
                obj['event_id'] = ApiClient.convertToType(data['event_id'], 'Number');
            }
            if (data.hasOwnProperty('listener_id')) {
                obj['listener_id'] = ApiClient.convertToType(data['listener_id'], 'Number');
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Number');
            }
            if (data.hasOwnProperty('trigger_type')) {
                obj['trigger_type'] = ApiClient.convertToType(data['trigger_type'], 'String');
            }
            if (data.hasOwnProperty('trigger_data')) {
                obj['trigger_data'] = ApiClient.convertToType(data['trigger_data'], Object);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Event</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Event</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Event.RequiredProperties) {
            if (!data[property]) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['trigger_type'] && !(typeof data['trigger_type'] === 'string' || data['trigger_type'] instanceof String)) {
            throw new Error("Expected the field `trigger_type` to be a primitive type in the JSON string but got " + data['trigger_type']);
        }

        return true;
    }


}

Event.RequiredProperties = ["event_id", "listener_id", "created_at", "trigger_type", "trigger_data"];

/**
 * @member {Number} event_id
 */
Event.prototype['event_id'] = undefined;

/**
 * @member {Number} listener_id
 */
Event.prototype['listener_id'] = undefined;

/**
 * @member {Number} created_at
 */
Event.prototype['created_at'] = undefined;

/**
 * @member {String} trigger_type
 */
Event.prototype['trigger_type'] = undefined;

/**
 * @member {Object} trigger_data
 */
Event.prototype['trigger_data'] = undefined;






export default Event;

