/**
 * BuiltByBit API
 * All operations not tagged 'free' require an active [Ultimate](https://builtbybit.com/account/ultimate) subscription or invite-only permissions.    V2 documentation: https://builtbybit.com/wiki/api-v2/ \\  OAuth2 documentation: https://builtbybit.com/wiki/oauth2/
 *
 * The version of the OpenAPI document: v2
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import DownloadPlanAction from './DownloadPlanAction';
import DownloadPlanNotice from './DownloadPlanNotice';

/**
 * The DownloadPlan model module.
 * @module model/DownloadPlan
 * @version v2
 */
class DownloadPlan {
    /**
     * Constructs a new <code>DownloadPlan</code>.
     * @alias module:model/DownloadPlan
     * @param level {module:model/DownloadPlan.LevelEnum} 
     */
    constructor(level) { 
        
        DownloadPlan.initialize(this, level);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, level) { 
        obj['level'] = level;
    }

    /**
     * Constructs a <code>DownloadPlan</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DownloadPlan} obj Optional instance to populate.
     * @return {module:model/DownloadPlan} The populated <code>DownloadPlan</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DownloadPlan();

            if (data.hasOwnProperty('level')) {
                obj['level'] = ApiClient.convertToType(data['level'], 'String');
            }
            if (data.hasOwnProperty('actions')) {
                obj['actions'] = ApiClient.convertToType(data['actions'], [DownloadPlanAction]);
            }
            if (data.hasOwnProperty('notices')) {
                obj['notices'] = ApiClient.convertToType(data['notices'], [DownloadPlanNotice]);
            }
            if (data.hasOwnProperty('unsupported')) {
                obj['unsupported'] = ApiClient.convertToType(data['unsupported'], ['String']);
            }
            if (data.hasOwnProperty('requires_empty_server')) {
                obj['requires_empty_server'] = ApiClient.convertToType(data['requires_empty_server'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DownloadPlan</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DownloadPlan</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of DownloadPlan.RequiredProperties) {
            if (!data[property]) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['level'] && !(typeof data['level'] === 'string' || data['level'] instanceof String)) {
            throw new Error("Expected the field `level` to be a primitive type in the JSON string but got " + data['level']);
        }
        if (data['actions']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['actions'])) {
                throw new Error("Expected the field `actions` to be an array in the JSON data but got " + data['actions']);
            }
            // validate the optional field `actions` (array)
            for (const item of data['actions']) {
                DownloadPlanAction.validateJSON(item);
            };
        }
        if (data['notices']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['notices'])) {
                throw new Error("Expected the field `notices` to be an array in the JSON data but got " + data['notices']);
            }
            // validate the optional field `notices` (array)
            for (const item of data['notices']) {
                DownloadPlanNotice.validateJSON(item);
            };
        }
        // ensure the json data is an array
        if (!Array.isArray(data['unsupported'])) {
            throw new Error("Expected the field `unsupported` to be an array in the JSON data but got " + data['unsupported']);
        }
        // ensure the json data is a string
        if (data['requires_empty_server'] && !(typeof data['requires_empty_server'] === 'string' || data['requires_empty_server'] instanceof String)) {
            throw new Error("Expected the field `requires_empty_server` to be a primitive type in the JSON string but got " + data['requires_empty_server']);
        }

        return true;
    }


}

DownloadPlan.RequiredProperties = ["level"];

/**
 * @member {module:model/DownloadPlan.LevelEnum} level
 */
DownloadPlan.prototype['level'] = undefined;

/**
 * @member {Array.<module:model/DownloadPlanAction>} actions
 */
DownloadPlan.prototype['actions'] = undefined;

/**
 * @member {Array.<module:model/DownloadPlanNotice>} notices
 */
DownloadPlan.prototype['notices'] = undefined;

/**
 * A list of features which were unsupported (not passed into the 'supported' query paramter) which were needed, resulting in the plan level being downgraded to 'manual'.
 * @member {Array.<String>} unsupported
 */
DownloadPlan.prototype['unsupported'] = undefined;

/**
 * Whether the server files must be empty before exeucting the plan steps.
 * @member {String} requires_empty_server
 */
DownloadPlan.prototype['requires_empty_server'] = undefined;





/**
 * Allowed values for the <code>level</code> property.
 * @enum {String}
 * @readonly
 */
DownloadPlan['LevelEnum'] = {

    /**
     * value: "full"
     * @const
     */
    "full": "full",

    /**
     * value: "partial"
     * @const
     */
    "partial": "partial",

    /**
     * value: "manual"
     * @const
     */
    "manual": "manual"
};



export default DownloadPlan;

